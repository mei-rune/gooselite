
test:
    driver: mssql
    open: dsn=tpt;uid=tpt;pwd=extreme

development:
    driver: mssql
    open: dsn=tpt;uid=tpt;pwd=extreme

production:
    driver: mssql
    open: dsn=tpt;uid=tpt;pwd=extreme

customimport:
    driver: customdriver
    open: customdriver open
    import: github.com/custom/driver
    dialect: mysql
